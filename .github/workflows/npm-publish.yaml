name: publish

on: [push]

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/setup-node
      - name: install
        run: npm install
      - name: test
        run: npm test
      - name: adduser
        run: npm login  --registry=https://npm.pkg.github.com/darafi/react-activity-feed/
      - name: publish
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: npm publish
